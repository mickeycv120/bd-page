import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["www.tecnm.mx", "assets.aceternity.com", "queretaro.tecnm.mx"],
  },
};

export default nextConfig;
